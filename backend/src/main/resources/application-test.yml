spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rankupdb_test
    username: rankupadmin
    password: rankupadmin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
      mode: always
